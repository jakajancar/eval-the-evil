#!/bin/bash
set -eu -o pipefail
cd "$(dirname $0)/.."

mkdir -p build
clang++ src/eval-the-evil.cc -o build/eval-the-evil \
    -pthread \
    -lstdc++ \
    -l:libv8_monolith.a \
    -l:libboost_program_options.a \
    -l:libboost_system.a
